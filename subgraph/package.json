{
  "name": "enigma-subgraph",
  "version": "0.0.1",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio enigma-sepolia",
    "create-local": "graph create --node http://localhost:8020 enigma-sepolia",
    "remove-local": "graph remove --node http://localhost:8020 enigma-sepolia",
    "deploy-local": "graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 enigma-sepolia"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.35.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.73.0",
    "typescript": "^5.4.5"
  }
}
